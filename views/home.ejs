<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Enter Your URL</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 2rem;
      background-color: #f5f5f5;
    }

    form {
      background-color: white;
      padding: 2rem;
      border-radius: 8px;
      max-width: 400px;
      margin: 0 auto;
      box-shadow: 0 0 10px rgba(0,0,0,0.1);
    }

    label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: bold;
    }

    input[type="url"] {
      width: 100%;
      padding: 0.5rem;
      border-radius: 4px;
      border: 1px solid #ccc;
      margin-bottom: 1rem;
    }

    button {
      padding: 0.5rem 1rem;
      border: none;
      background-color: #28a745;
      color: white;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      background-color: #218838;
    }
  </style>
</head>
<body>
   <% if(locals.id){%>
    <p>URL generated:http://localhost:8001/url/<%= id %></p>
   <% }%>
  <h1>Submit Your URL</h1>
  <form action="/url" method="POST">
    <label for="userUrl">Enter URL:</label>
    <input type="url" id="userUrl" name="url" placeholder="https://example.com" required>
    <button type="submit">Submit</button>
  </form>

  <div>
    <% if(locals.urls) {%>
    <table>
        <thead>
            <th>s.no</th>
            <th>short ID</th>
            <th>redirect </th>
            <th>views</th>

        </thead>
        <tbody>
            <% urls.forEach((url,index)=> { %>
               <tr>
              <td><%= index+1 %></td>
              <td><%= url.shortId %></td> 
              <td><%= url.redirectURL %></td>
              <td><%= url.visitHistory.length %></td>               
           <%  });%> 
               </tr>  
        </tbody>

    </table>
    <% } %>
  </div>
</body>
</html>
